# UI 表示確認 テスト計画

**Seed:** `e2e/seed.spec.ts`

---

## ET100: ログイン画面の初期表示

**前提条件:** 未認証で `/login` を開く。

**確認内容:**

- 見出し「日報システム」が表示されている
- ラベル「メールアドレス」が表示されている
- ラベル「パスワード」が表示されている
- ボタン「ログイン」が表示されている

---

## ET101: SALES の日報一覧画面の初期表示

**前提条件:** `sales1@test.com` でログイン済み。`/reports` を開く。

**確認内容:**

- 見出し「日報一覧」が表示されている
- 「+ 日報を作成」ボタンが表示されている
- ステータスフィルターが表示されている
- 日付範囲フィルターが表示されている（初期値が直近 30 日）
- 列ヘッダー「日付」「ステータス」が表示されている

---

## ET102: isAdmin=false の SALES ではナビに管理者リンクが表示されない

**前提条件:** `sales1@test.com`（isAdmin=false）でログイン済み。

**確認内容:**

- ナビゲーションに「日報一覧」リンクが表示されている
- ナビゲーションに「顧客マスタ」リンクが表示されていない
- ナビゲーションに「営業マスタ」リンクが表示されていない

---

## ET103: isAdmin=true のユーザーのナビには管理者リンクが表示される

**前提条件:** `manager1@test.com`（isAdmin=true）でログイン済み。

**確認内容:**

- ナビゲーションに「日報一覧」リンクが表示されている
- ナビゲーションに「顧客マスタ」リンクが表示されている
- ナビゲーションに「営業マスタ」リンクが表示されている

---

## ET104: 日報作成画面の初期表示

**前提条件:** `sales1@test.com` でログイン済み。`/reports/new` を開く。

**確認内容:**

- 見出し「日報作成」が表示されている
- 日付フィールドに「日付 \*」の必須マーカーが表示されている
- 日付の初期値が今日の日付になっている
- 「+ 訪問記録を追加」ボタンが表示されている
- 「キャンセル」「下書き保存」「提出」ボタンが表示されている

---

## ET105: 日報編集画面には既存データが初期値として表示される

**前提条件:** `sales1@test.com` でログイン済み。DRAFT 日報（当日-4）の編集画面を開く。

**確認内容:**

- 見出し「日報編集」が表示されている
- 日付フィールドに既存の日付が初期値として表示されている
- 既存の Problem・Plan の内容が各テキストエリアに表示されている

---

## ET106: MANAGER の日報一覧画面のフィルター初期値

**前提条件:** `manager1@test.com` でログイン済み。`/manager/reports` を開く。

**確認内容:**

- 見出し「部下の日報一覧」が表示されている
- ステータスフィルターの初期値が「提出済」と表示されている
- 営業フィルターの初期値が「全員」と表示されている
- 列ヘッダー「日付」「営業氏名」「ステータス」が表示されている

---

## ET107: MANAGER の日報一覧で「全て」フィルターに変更すると件数が増える

**前提条件:** `manager1@test.com` でログイン済み。`/manager/reports` を開く。

**手順:**

1. ステータスフィルターを「全て」に変更する

**期待結果:** DRAFT 以外の全ステータスの日報が表示される。初期値「提出済」より件数が増える（REJECTED や COMPLETED の日報も表示される）

---

## ET108: SUBMITTED 日報詳細（上長）にコメント欄と操作ボタンが表示される

**前提条件:** `manager1@test.com` でログイン済み。SUBMITTED 日報（当日-3）の詳細画面を開く。

**確認内容:**

- 「差し戻し」「承認」ボタンが表示されている
- Problem セクションにコメント入力欄と「送信」ボタンが表示されている
- Plan セクションにコメント入力欄と「送信」ボタンが表示されている
- 全般コメントセクションにコメント入力欄と「送信」ボタンが表示されている

---

## ET109: 顧客追加画面の初期表示

**前提条件:** `manager1@test.com`（isAdmin=true）でログイン済み。`/admin/customers/new` を開く。

**確認内容:**

- 見出し「顧客追加」が表示されている
- 「顧客名 \*」に必須マーカーが表示されている
- 住所・備考には必須マーカーが表示されていない
- 「キャンセル」「保存」ボタンが表示されている

---

## ET110: 顧客編集画面には既存データが初期値として表示される

**前提条件:** `manager1@test.com`（isAdmin=true）でログイン済み。テスト顧客Aの編集画面を開く。

**確認内容:**

- 見出し「顧客編集」が表示されている
- 顧客名欄に「テスト顧客A」が初期値として入力されている

---

## ET111: 営業追加画面の初期表示と SALES デフォルト選択

**前提条件:** `manager1@test.com`（isAdmin=true）でログイン済み。`/admin/users/new` を開く。

**確認内容:**

- 見出し「営業追加」が表示されている
- 「氏名 \*」「メールアドレス \*」「ロール \*」に必須マーカーが表示されている
- 「SALES」がデフォルト選択されている
- 「上長 \*」フィールドが表示されている（SALES デフォルト選択のため）

---

## ET112: 営業編集画面ではメールアドレスが読み取り専用

**前提条件:** `manager1@test.com`（isAdmin=true）でログイン済み。山田太郎の編集画面を開く。

**確認内容:**

- 見出し「営業編集」が表示されている
- メールアドレス欄が読み取り専用（編集不可）になっている
- 「パスワードをリセット」ボタンが表示されている
- 既存の氏名・ロール・上長情報が各フィールドに表示されている

---

## ET113: パスワード変更画面の初期表示

**前提条件:** いずれかのロールでログイン済み。`/settings/password` を開く。

**確認内容:**

- 見出し「パスワード変更」が表示されている
- 「現在のパスワード \*」に必須マーカーが表示されている
- 「新しいパスワード \*」に必須マーカーが表示されている
- 「新しいパスワード（確認） \*」に必須マーカーが表示されている
- 「キャンセル」「変更する」ボタンが表示されている

---

## ET114: ヘッダーのユーザー名クリックでドロップダウンが表示される

**前提条件:** `sales1@test.com` でログイン済み。

**手順:**

1. ヘッダーのユーザー名「山田太郎」をクリックする

**期待結果:** ドロップダウンメニューが表示される。「パスワード変更」リンクが含まれる

---

## ET115: SALES の「日報一覧」ナビリンクをクリックすると `/reports` に遷移する

**前提条件:** `sales1@test.com` でログイン済み。

**手順:**

1. ヘッダー（ナビゲーション）の「日報一覧」リンクをクリックする

**期待結果:** `/reports` に遷移する

---

## ET116: MANAGER の「日報一覧」ナビリンクをクリックすると `/manager/reports` に遷移する

**前提条件:** `manager1@test.com` でログイン済み。

**手順:**

1. ヘッダー（ナビゲーション）の「日報一覧」リンクをクリックする

**期待結果:** `/manager/reports` に遷移する
