# Database
# docker compose up -d db で起動したローカルPostgresを使う場合はこのままでOK
# Neonなど別のPostgreSQLを使う場合はそちらの接続文字列に置き換える
DATABASE_URL="postgresql://test:test@localhost:5432/daily_report_test"

# Auth.js
# ランダムな文字列を生成して設定する（例: openssl rand -base64 32）
AUTH_SECRET="change-me"
AUTH_URL="http://localhost:3000"
